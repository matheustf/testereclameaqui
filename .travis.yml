language: java

  
jdk:
  - oraclejdk8
  - openjdk8

services: mongodb

after_success:
  - mvn clean cobertura:cobertura coveralls:report
  - bash <(curl -s https://codecov.io/bash)

notifications: 
    email:
        - teles.matheus@hotmail.com #Add your email address
script:
  - mvn -f spring-boot-starter-parent/pom.xml clean verify -Pjacoco coveralls:report
